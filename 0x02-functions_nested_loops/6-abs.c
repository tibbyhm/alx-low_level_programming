#include "main.h"
/**
* _abs - prints absolute value of a number
*
* @n: input parameter that is being checked
*
* Return: 0
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
		return (n);
}
