#include "main.h"
#include <string.h>

/**
* _strlen - Uses str len to find length of a string
* @s: Inputted string
*
* Return: Length of the string
*/

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
