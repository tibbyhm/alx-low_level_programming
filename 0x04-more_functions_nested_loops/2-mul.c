#include "main.h"

/**
 * mul - multiplies two numbers.
 *
 * @a: First Input parameter that is being checked
 * @b: First Input parameter that is being checked
 *
 * Return: product of two numbers
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
